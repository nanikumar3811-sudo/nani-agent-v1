# Security

Provider credentials remain server-side. Do not expose environment variables to Vite client code. LIVE_TRADING is permanently disabled in the current product boundary. Validate all external inputs before adding new API routes, and never commit secret values.
