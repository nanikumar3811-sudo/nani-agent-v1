# AI Boundary

The AI layer must receive versioned evidence packets only. It may explain supporting/contradicting evidence and uncertainty, but it cannot create market facts. Phase 1 uses deterministic evidence and does not require an LLM API key.
