# Deployment

The repository retains Docker and Render configuration. Phase 1 is safe to run in DEMO mode without paid provider credentials. Production deployment requires a PostgreSQL connection and should use environment variables for all secrets.

Do not claim a Render service is provisioned until deployment has actually completed.
