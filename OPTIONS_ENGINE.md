# Options Engine

Options architecture is intentionally not expanded in Phase 1. The existing research-only endpoint remains fail-closed when underlying/options data cannot be validated. Trade-level flow, IV, GEX, and contract fixtures will be introduced only with normalized provider contracts and tests.
