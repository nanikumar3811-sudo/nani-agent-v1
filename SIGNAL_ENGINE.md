# Signal Engine

The Phase 1 deterministic engine exposes technical state, market regime, and evidence packets. Scores are explainable inputs, not probabilities or guarantees. Missing data reduces confidence and unavailable evidence is disclosed.
