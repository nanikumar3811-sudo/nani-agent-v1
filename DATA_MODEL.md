# Data Model

Phase 1 migrations establish symbols, market snapshots, technical snapshots, provider health, and evidence packets. Payloads retain normalized JSON for provider provenance while indexed columns support common lookups.

Future domain tables should be added by migration rather than ad-hoc startup DDL.
